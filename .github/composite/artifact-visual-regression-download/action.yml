name: 📥 Visual Regression Artifacts · Download

runs:
  using: 'composite'
  steps:
    - name: 📥 Visual Regression Artifacts · Download
      uses: actions/download-artifact@v7
      with:
        name: artifacts-visual-regression
        path: artifacts/visual-regression
