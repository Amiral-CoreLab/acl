name: 🧪 Tests

runs:
  using: 'composite'
  steps:
    - name: 🧪 Tests · TypeScript
      shell: bash
      run: npx ng run acl:test --watch=false --coverage

    - name: 📊 Tests · Coverage Report
      shell: bash
      run: echo "Coverage generated"
