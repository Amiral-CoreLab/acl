name: 📦 Visual Regression Artifacts · Upload

runs:
  using: 'composite'
  steps:
    - name: 📦 Visual Regression Artifacts · Upload
      uses: actions/upload-artifact@v6
      with:
        name: artifacts-visual-regression
        path: artifacts/visual-regression
